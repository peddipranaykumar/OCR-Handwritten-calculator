To run the application, follow these steps:

1. Open a new terminal.
2. Enter the command `python app.py`.
3. Once the application is running, open the URL displayed in the terminal.
4. Click on the URL to open the Flask website.